      properties:
        date: { $ref: "#/components/schemas/JournalDate" }
        description: { type: string, minLength: 1 }
        lines:
          type: array
          minItems: 2
          items: { $ref: "#/components/schemas/JournalEntryLineInput" }
    JournalEntryUpdateInput:
      type: object
      required: [lines]
      properties:
        lines:
          type: array
          minItems: 2
          items: { $ref: "#/components/schemas/JournalEntryLineInput" }
    JournalEntrySummary:
      type: object
      required: [id, date, description, sourceType, sourceId, status, createdBy, createdAt, totalDebit, totalCredit]
      properties:
        id: { type: integer }
        date: { $ref: "#/components/schemas/JournalDate" }
        description: { type: string }
        sourceType: { type: string }
        sourceId: { type: ["integer", "null"] }
        status: { type: string, enum: [draft, posted, void] }
        createdBy: { type: ["integer", "null"] }
        createdAt: { type: string, format: date-time }
        totalDebit: { type: number }
        totalCredit: { type: number }
    JournalEntry:
      allOf:
        - $ref: "#/components/schemas/JournalEntrySummary"
        - type: object
          required: [lines, voidedAt, voidedBy]
          properties:
            lines:
              type: array
              items: { $ref: "#/components/schemas/JournalEntryLine" }
            voidedAt: { type: ["string", "null"], format: date-time }
            voidedBy: { type: ["integer", "null"] }
    JournalVoidResult:
      type: object
      required: [reversalEntryId]
      properties:
        reversalEntryId: { type: integer }
    JournalLedger:
      type: object
      required: [accountId, code, name, normalBalance, entries]
      properties:
        accountId: { type: integer }
        code: { type: string }
        name: { type: string }
        normalBalance: { type: string, enum: [debit, credit] }
        entries:
          type: array
          items: { $ref: "#/components/schemas/JournalLedgerEntry" }
    JournalLedgerEntry:
      type: object
      required: [date, journalEntryId, debit, credit, balance]
      properties:
        date: { $ref: "#/components/schemas/JournalDate" }
        journalEntryId: { type: integer }
        debit: { type: number }
        credit: { type: number }
        balance: { type: number }
    JournalTrialBalance:
      type: object
      required: [balanced, totalDebit, totalCredit, accounts]
      properties:
        balanced: { type: boolean }
        totalDebit: { type: number }
        totalCredit: { type: number }
        accounts:
          type: array
          items: { $ref: "#/components/schemas/JournalTrialBalanceAccount" }
    JournalTrialBalanceAccount:
      type: object
      required: [accountId, code, name, normalBalance, debit, credit, balance]
      properties:
        accountId: { type: integer }
        code: { type: string }
        name: { type: string }
        normalBalance: { type: string, enum: [debit, credit] }
        debit: { type: number }
        credit: { type: number }
        balance: { type: number }
    AuthSession:
      type: object
      required: [authenticated, id, role, username]
      properties:
        authenticated: { type: boolean }
        id: { type: [integer, "null"] }
        role: { type: [string, "null"], enum: [admin, hr, accountant, warehouse, viewer, null] }
        username: { type: [string, "null"] }
    User:
      type: object
      required: [id, username, role]
      properties:
        id: { type: integer }
        username: { type: string }
        role: { type: string, enum: [admin, hr, accountant, warehouse, viewer] }
    UserUpdateInput:
      type: object
      required: [username, role]
      properties:
        username: { type: string, minLength: 1 }
        role: { type: string, enum: [admin, hr, accountant, warehouse, viewer] }
        newPassword: { type: string, minLength: 1 }
    ChartOfAccount:
      type: object
      required: [id, code, name, type, isActive, createdAt]
      properties:
        id: { type: integer }
        code: { type: string }
        name: { type: string }
        type: { type: string, enum: [asset, liability, equity, revenue, expense] }
        isActive: { type: boolean }
        createdAt: { type: string, format: date-time }
    ChartOfAccountInput:
      type: object
      required: [code, name, type]
      properties:
        code: { type: string, minLength: 1, pattern: '^[0-9]+$' }
        name: { type: string, minLength: 1 }
        type: { type: string, enum: [asset, liability, equity, revenue, expense] }
    LoginInput:
      type: object
      required: [username, password]
      properties:
        username: { type: string }
        password: { type: string }
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    Dashboard:
      type: object
      required: [employeeCount, previousMonthSalesIncome, previousMonthPayrollExpense, previousMonthInventoryExpense, recentActivity]
      properties:
        employeeCount: { type: integer }
        previousMonthSalesIncome: { type: number }
        previousMonthPayrollExpense: { type: number }
        previousMonthInventoryExpense: { type: number }
        recentActivity:
          type: array
          items:
            $ref: "#/components/schemas/Activity"
    Activity:
      type: object
      required: [id, type, title, detail, createdAt]
      properties:
        id: { type: string }
        type: { type: string }
        title: { type: string }
        detail: { type: string }
        createdAt: { type: string }
    Employee:
      type: object
      required: [id, name, role, phone, salaryType, employeeType, baseSalary, socialInsuranceSalary, payrollTaxExempt, fullSalaryRegardlessAttendance, payFrequency, monthlyExpectedWorkDays, status, joinedAt, inactiveAt]
      properties:
        id: { type: integer }
        name: { type: string }
        role: { type: string }
        phone: { type: string }
        salaryType: { type: string, enum: [daily, monthly] }
        employeeType: { type: string, enum: [shift, office] }
        baseSalary: { type: number }
        socialInsuranceSalary: { type: number }
        payrollTaxExempt: { type: boolean }
        fullSalaryRegardlessAttendance: { type: boolean }
        payFrequency: { type: string, enum: [once, twice] }
        monthlyExpectedWorkDays: { type: integer }
        status: { type: string, enum: [active, inactive] }
        joinedAt: { type: string }
        inactiveAt: { type: [string, "null"] }
    EmployeeSalaryHistory:
      type: object
      required: [id, employeeId, effectiveFrom, employeeType, salaryType, monthlyExpectedWorkDays, baseSalary, socialInsuranceSalary, payrollTaxExempt, fullSalaryRegardlessAttendance, payFrequency, createdAt]
      properties:
        id: { type: integer }
        employeeId: { type: integer }
        effectiveFrom: { type: string }
        employeeType: { type: string, enum: [shift, office] }
        salaryType: { type: string, enum: [daily, monthly] }
        monthlyExpectedWorkDays: { type: integer }
        baseSalary: { type: number }
        socialInsuranceSalary: { type: number }
        payrollTaxExempt: { type: boolean }
        fullSalaryRegardlessAttendance: { type: boolean }
        payFrequency: { type: string, enum: [once, twice] }
        createdAt: { type: string }
    EmployeeSalaryHistoryUpdate:
      type: object
      required: [effectiveFrom, baseSalary, socialInsuranceSalary]
      properties:
        effectiveFrom: { type: string, format: date }
        baseSalary: { type: number, minimum: 0 }
        socialInsuranceSalary: { type: number, minimum: 0 }
        fullSalaryRegardlessAttendance: { type: boolean }
        payFrequency: { type: string, enum: [once, twice] }
        salaryType: { type: string, enum: [daily, monthly] }
        monthlyExpectedWorkDays: { type: integer, minimum: 0, maximum: 31 }
    EmployeeInput:
      type: object
      required: [name, role, phone, employeeType, baseSalary, socialInsuranceSalary, payrollTaxExempt, payFrequency, monthlyExpectedWorkDays, joinedAt]
      properties:
        name: { type: string, minLength: 1 }
        role: { type: string, minLength: 1 }
        phone: { type: string }
        employeeType: { type: string, enum: [shift, office] }
        salaryType: { type: string, enum: [daily, monthly] }
        baseSalary: { type: number, minimum: 0 }
        socialInsuranceSalary: { type: number, minimum: 0 }
        payrollTaxExempt: { type: boolean }
        fullSalaryRegardlessAttendance: { type: boolean }
        payFrequency: { type: string, enum: [once, twice] }
        monthlyExpectedWorkDays: { type: integer, minimum: 0, maximum: 31 }
        joinedAt: { type: string, format: date }
    EmployeeUpdate:
      type: object
      properties:
        name: { type: string, minLength: 1 }
        role: { type: string, minLength: 1 }
        phone: { type: string }
        employeeType: { type: string, enum: [shift, office] }
        salaryType: { type: string, enum: [daily, monthly] }
        monthlyExpectedWorkDays: { type: integer, minimum: 0, maximum: 31 }
        baseSalary: { type: number, minimum: 0 }
        socialInsuranceSalary: { type: number, minimum: 0 }
        payrollTaxExempt: { type: boolean }
        fullSalaryRegardlessAttendance: { type: boolean }
        payFrequency: { type: string, enum: [once, twice] }
        status: { type: string, enum: [active, inactive] }
        joinedAt: { type: string, format: date }
        inactiveAt: { type: [string, "null"] }
        salaryEffectiveDate: { type: string, format: date }
    Attendance:
      type: object
      required: [id, employeeId, employeeName, date, clockIn, clockOut, hours, status]
      properties:
        id: { type: integer }
        employeeId: { type: integer }
        employeeName: { type: string }
        date: { type: string }
        clockIn: { type: string }
        clockOut: { type: string }
        hours: { type: number }
        status: { type: string, enum: [present, late, leave, absent] }
    AttendanceInput:
      type: object
      required: [employeeId, date, clockIn, clockOut, status]
      properties:
        employeeId: { type: integer }
        date: { type: string }
        clockIn: { type: string }
        clockOut: { type: string }
        status: { type: string, enum: [present, late, leave, absent] }
    AttendanceToggleInput:
      type: object
      required: [employeeId, date, status]
      properties:
        employeeId: { type: integer }
        date: { type: string }
        status: { type: string, enum: [present, absent, leave] }
        clockIn: { type: string }
        clockOut: { type: string }
        hours: { type: number, minimum: 0 }
    Shift:
      type: object
      required: [id, name, startTime, endTime]
      properties:
        id: { type: integer }
        name: { type: string }
        startTime: { type: string }
        endTime: { type: string }
    ShiftInput:
      type: object
      required: [name, startTime, endTime]
      properties:
        name: { type: string, minLength: 1 }
        startTime: { type: string, pattern: '^([01]\d|2[0-3]):[0-5]\d$' }
        endTime: { type: string, pattern: '^([01]\d|2[0-3]):[0-5]\d$' }
    ShiftPlan:
      type: object
      required: [id, employeeId, employeeName, date, shiftId, shiftName, startTime, endTime]
      properties:
        id: { type: integer }
        employeeId: { type: integer }
        employeeName: { type: string }
        date: { type: string }
        shiftId: { type: integer }
        shiftName: { type: string }
        startTime: { type: string }
        endTime: { type: string }
    ShiftPlanInput:
      type: object
      required: [employeeId, date, shiftId]
      properties:
        employeeId: { type: integer }
        date:
          type: string
          pattern: '^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$'
        shiftId: { type: ["integer", "null"] }
    ShiftPlanCopyInput:
      type: object
      required: [sourceMonth, month, overwrite]
      properties:
        sourceMonth: { type: string, pattern: '^\d{4}-(0[1-9]|1[0-2])$' }
        month: { type: string, pattern: '^\d{4}-(0[1-9]|1[0-2])$' }
        overwrite: { type: boolean }
    ShiftPlanCopyResult:
      type: object
      required: [sourceMonth, targetMonth, copied, overwritten, skipped, unavailableDates]
      properties:
        sourceMonth: { type: string }
        targetMonth: { type: string }
        copied: { type: integer }
        overwritten: { type: integer }
        skipped: { type: integer }
        unavailableDates: { type: integer }
    HourBalanceLine:
      type: object
      required: [employeeId, employeeName, role, month, totalHours, expectedWorkDays, workDays, eightHourDays, twelveHourDays, leaveDays]
      properties:
        employeeId: { type: integer }
        employeeName: { type: string }
        role: { type: string }
        month: { type: string }
        totalHours: { type: number }
        expectedWorkDays: { type: integer }
        workDays: { type: integer }
        eightHourDays: { type: integer }
        twelveHourDays: { type: integer }
        leaveDays: { type: integer }
    PayrollLine:
      type: object
      required: [employeeId, employeeName, role, employeeType, daysWorked, hours, gross, socialInsuranceSalary, socialInsurance, taxableIncome, calculatedIncomeTax, taxRelief, incomeTax, advanceAmount, manualDeduction, deductions, carryoverAmount, payable, paidAmount, paymentDate, secondPaidAmount, secondPaymentDate, remainingAmount, overpaidAmount, balanceAmount, net]
      properties:
        employeeId: { type: integer }
        employeeName: { type: string }
        role: { type: string }
        employeeType: { type: string, enum: [shift, office] }
        daysWorked: { type: number }
        hours: { type: number }
        gross: { type: number }
        socialInsuranceSalary: { type: number }
        socialInsurance: { type: number }
        taxableIncome: { type: number }
        calculatedIncomeTax: { type: number }
        taxRelief: { type: number }
        incomeTax: { type: number }
        advanceAmount: { type: number }
        manualDeduction: { type: number }
        deductions: { type: number }
        carryoverAmount: { type: number }
        payable: { type: number }
        paidAmount: { type: number }
        paymentDate: { type: ["string", "null"] }
        secondPaidAmount: { type: number }
        secondPaymentDate: { type: ["string", "null"] }
        remainingAmount: { type: number }
        overpaidAmount: { type: number }
        balanceAmount: { type: number }
        net: { type: number }
    PayrollAdjustment:
      type: object
      required: [employeeId, month, taxRelief, manualDeduction, paidAmount, secondPaidAmount]
      properties:
        employeeId: { type: integer }
        month: { type: string }
        taxRelief: { type: number }
        manualDeduction: { type: number }
        paidAmount: { type: number }
        paymentDate: { type: ["string", "null"] }
        secondPaidAmount: { type: number }
        secondPaymentDate: { type: ["string", "null"] }
    PayrollAdjustmentInput:
      type: object
      required: [employeeId, month, manualDeduction, paidAmount, secondPaidAmount]
      properties:
        employeeId: { type: integer }
        month: { type: string, pattern: '^\d{4}-(0[1-9]|1[0-2])$' }
        manualDeduction: { type: number, minimum: 0 }
        paidAmount: { type: number, minimum: 0 }
        paymentDate: { type: ["string", "null"] }
        secondPaidAmount: { type: number, minimum: 0 }
        secondPaymentDate: { type: ["string", "null"] }
    PayrollSummary:
      type: object
      required: [month, periodStart, advancePeriodEnd, periodEnd, advancePaymentDate, finalPaymentDate, schedule, totalGross, totalSocialInsurance, totalIncomeTax, totalDeductions, totalNet, lines]
      properties:
        month: { type: string }
        periodStart: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        advancePeriodEnd: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        periodEnd: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        advancePaymentDate: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        finalPaymentDate: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        schedule: { $ref: "#/components/schemas/PayrollSchedule" }
        totalGross: { type: number }
        totalSocialInsurance: { type: number }
        totalIncomeTax: { type: number }
        totalDeductions: { type: number }
        totalNet: { type: number }
        lines:
          type: array
          items:
            $ref: "#/components/schemas/PayrollLine"
    PayrollAdvanceLine:
      type: object
      required: [employeeId, employeeName, employeeType, baseSalary, daysWorked, dailySalary, totalSalary, advanceAmount, paid]
      properties:
        employeeId: { type: integer }
        employeeName: { type: string }
        employeeType: { type: string, enum: [shift, office] }
        baseSalary: { type: number }
        daysWorked: { type: number }
        dailySalary: { type: number }
        totalSalary: { type: number }
        advanceAmount: { type: number }
        paid: { type: boolean }
        paymentDate: { type: ["string", "null"] }
    PayrollAdvanceSummary:
      type: object
      required: [month, periodStart, advancePeriodEnd, periodEnd, advancePaymentDate, finalPaymentDate, schedule, approved, approvalDate, totalAmount, lines]
      properties:
        month: { type: string }
        periodStart: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        advancePeriodEnd: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        periodEnd: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        advancePaymentDate: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        finalPaymentDate: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        schedule: { $ref: "#/components/schemas/PayrollSchedule" }
        approved: { type: boolean }
        approvalDate: { type: ["string", "null"] }
        approvedAt: { type: string }
        totalAmount: { type: number }
        lines:
          type: array
          items:
            $ref: "#/components/schemas/PayrollAdvanceLine"
    PayrollAdvanceApprovalInput:
      type: object
      required: [month, approvalDate]
      properties:
        month: { type: string, pattern: '^\d{4}-(0[1-9]|1[0-2])$' }
        approvalDate: { type: string, pattern: '^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$' }
    PayrollAdvancePaymentInput:
      type: object
      required: [month, employeeId, advanceAmount, paid]
      properties:
        month: { type: string, pattern: '^\d{4}-(0[1-9]|1[0-2])$' }
        employeeId: { type: integer }
        advanceAmount: { type: number, minimum: 0 }
        paid: { type: boolean }
        paymentDate: { type: ["string", "null"] }
    PayrollSchedule:
      type: object
      required: [id, effectiveFromMonth, periodStartDay, advanceCutoffDay, periodEndDay, advancePayDay, finalPayDay]
      properties:
        id: { type: integer }
        effectiveFromMonth: { type: string, pattern: '^\d{4}-(0[1-9]|1[0-2])$' }
        periodStartDay: { type: integer, minimum: 1, maximum: 31 }
        advanceCutoffDay: { type: integer, minimum: 1, maximum: 31 }
        periodEndDay: { type: integer, minimum: 1, maximum: 31 }
        advancePayDay: { type: integer, minimum: 1, maximum: 31 }
        finalPayDay: { type: integer, minimum: 1, maximum: 31 }
    PayrollScheduleInput:
      type: object
      required: [periodStartDay, advanceCutoffDay, periodEndDay, advancePayDay, finalPayDay]
      properties:
        periodStartDay: { type: integer, minimum: 1, maximum: 31 }
        advanceCutoffDay: { type: integer, minimum: 1, maximum: 31 }
        periodEndDay: { type: integer, minimum: 1, maximum: 31 }
        advancePayDay: { type: integer, minimum: 1, maximum: 31 }
        finalPayDay: { type: integer, minimum: 1, maximum: 31 }
    CashTransaction:
      type: object
      required: [id, type, category, subcategory, accountId, accountCode, accountName, description, amount, date, incomeMonth, bankTransactionId, bankVerifiedAt, createdAt, editable, transactionKind]
      properties:
        id: { type: integer }
        type: { type: string, enum: [income, expense] }
        category: { type: string }
        subcategory: { type: ["string", "null"] }
        accountId: { type: ["integer", "null"] }
        accountCode: { type: ["string", "null"] }
        accountName: { type: ["string", "null"] }
        description: { type: string }
        amount: { type: number }
        date: { type: string }
        incomeMonth: { type: ["string", "null"], pattern: '^\d{4}-(0[1-9]|1[0-2])$' }
        bankTransactionId: { type: ["integer", "null"] }
        bankVerifiedAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string }
        editable: { type: boolean }
        transactionKind: { type: string, enum: [manual, payroll, payroll_advance, inventory_purchase, fixed_asset_purchase, operating_expense, bank_transaction] }
    BankAccount:
      type: object
      required: [id, bankName, accountNumber, createdAt]
      properties:
        id: { type: integer }
        bankName: { type: string }
        accountNumber: { type: string }
        createdAt: { type: string, format: date-time }
    BankAccountInput:
      type: object
      required: [bankName, accountNumber]
      properties:
        bankName: { type: string, minLength: 1, maxLength: 100 }
        accountNumber: { type: string, minLength: 1, maxLength: 100 }
    UnclearTransaction:
      type: object
      required: [id, source, type, description, amount, occurredAt, unclearAt]
      properties:
        id: { type: integer }
        source: { type: string, enum: [bank, cash] }
        type: { type: string, enum: [income, expense] }
        description: { type: string }
        amount: { type: number }
        occurredAt: { type: string }
        account: { type: ["string", "null"] }
        category: { type: ["string", "null"] }
        unclearAt: { type: string, format: date-time }
    UnclearTransactionList:
      type: array
      items: { $ref: "#/components/schemas/UnclearTransaction" }
    BankTransactionCashTransferInput:
      type: object
      required: [category, incomeMonth]
      properties:
        category: { type: string, minLength: 1, maxLength: 200 }
        incomeMonth: { type: ["string", "null"], pattern: '^\d{4}-(0[1-9]|1[0-2])$' }
    BankTransactionCashLinkInput:
      type: object
      required: [cashTransactionId]
      properties:
        cashTransactionId: { type: integer, minimum: 1 }
    CashTransactionSuggestion:
      type: object
      required: [id, type, category, subcategory, description, amount, date, bankTransactionId, bankVerifiedAt, createdAt, editable, transactionKind, score]
      properties:
        id: { type: integer }
        type: { type: string, enum: [income, expense] }
        category: { type: string }
        subcategory: { type: ["string", "null"] }
        description: { type: string }
        amount: { type: number }
        date: { type: string }
        bankTransactionId: { type: ["integer", "null"] }
        bankVerifiedAt: { type: ["string", "null"], format: date-time }
        createdAt: { type: string }
        editable: { type: boolean }
        transactionKind: { type: string, enum: [manual, payroll, payroll_advance, inventory_purchase, fixed_asset_purchase, operating_expense, bank_transaction] }
        score: { type: number }
    CashTransactionInput:
      type: object
      required: [type, category, description, amount, date, incomeMonth]
      properties:
        type: { type: string, enum: [income, expense] }
        category: { type: string, minLength: 1 }
        description: { type: string, minLength: 1 }
        amount: { type: number, minimum: 0 }
        date: { type: string }
        incomeMonth: { type: ["string", "null"], pattern: '^\d{4}-(0[1-9]|1[0-2])$' }
    CashIncomeMonthUpdate:
      type: object
      required: [incomeMonth]
      properties:
        incomeMonth: { type: string, pattern: '^\d{4}-(0[1-9]|1[0-2])$' }
    CashClosure:
      type: object
      required: [id, date, closedAt]
      properties:
        id: { type: integer }
        date: { type: string }
        closedAt: { type: string }
    CashClosureInput:
      type: object
      required: [date]
      properties:
        date:
          type: string
          pattern: '^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$'
    CashSummary:
      type: object
      required: [balance, income, expense, todayIncome, todayExpense]
      properties:
        balance: { type: number }
        income: { type: number }
        expense: { type: number }
        todayIncome: { type: number }
        todayExpense: { type: number }
    BankTransaction:
      type: object
      required: [id, transactionAt, type, amount, accountId, accountCode, accountName, account, counterparty, description, executedAt, balance, transferredAt, cashTransactionId, bankAccountId, bankName, bankAccountNumber, createdAt]
      properties:
        id: { type: integer }
        transactionAt: { type: string, format: date-time }
        type: { type: string, enum: [income, expense] }
        amount: { type: number }
        accountId: { type: ["integer", "null"] }
        accountCode: { type: ["string", "null"] }
        accountName: { type: ["string", "null"] }
        account: { type: string }
        counterparty: { type: string }
        description: { type: string }
        executedAt: { type: ["string", "null"], format: date-time }
        balance: { type: ["number", "null"] }
        transferredAt: { type: ["string", "null"], format: date-time }
        cashTransactionId: { type: ["integer", "null"] }
        bankAccountId: { type: ["integer", "null"] }
        bankName: { type: ["string", "null"] }
        bankAccountNumber: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
    BankTransactionAccountInput:
      type: object
      required: [accountId]
      properties:
        accountId: { type: ["integer", "null"], minimum: 1 }
    BankTransactionJournalReviewItem:
      type: object
      required: [id, date, transactionAt, type, description, amount, counterparty, suggestedAccountId, suggestedAccountName]
      properties:
        id: { type: integer }
        date: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
        transactionAt: { type: string, format: date-time }
        type: { type: string, enum: [income, expense] }
        description: { type: string }
        amount: { type: number }
        counterparty: { type: string }
        suggestedAccountId: { type: ["integer", "null"] }
        suggestedAccountName: { type: ["string", "null"] }
        existingPurchaseMatch:
          $ref: "#/components/schemas/BankTransactionPurchaseMatch"
    BankTransactionPurchaseMatch:
      type: object
      required: [type, id]
      properties:
        type: { type: string, enum: [inventory_purchase, operating_expense] }
        id: { type: integer, minimum: 1 }
    ExistingInventoryPurchaseLink:
      type: object
      required: [inventoryPurchaseId]
      properties:
        inventoryPurchaseId: { type: integer, minimum: 1 }
    BankPurchaseLinkInput:
      anyOf:
        - $ref: "#/components/schemas/ExistingInventoryPurchaseLink"
        - $ref: "#/components/schemas/InventoryPurchaseInput"
    BankPurchaseLinkResult:
      type: object
      required: [bankTransactionId, inventoryPurchaseId, cashTransactionId, journalEntryId]
      properties:
        bankTransactionId: { type: integer, minimum: 1 }
        inventoryPurchaseId: { type: integer, minimum: 1 }
        cashTransactionId: { type: integer, minimum: 1 }
        journalEntryId: { type: integer, minimum: 1 }
    ExistingOperatingExpenseLink:
      type: object
      required: [operatingExpenseId]
      properties:
        operatingExpenseId: { type: integer, minimum: 1 }
    BankExpenseLinkInput:
      anyOf:
        - $ref: "#/components/schemas/ExistingOperatingExpenseLink"
        - $ref: "#/components/schemas/OperatingExpenseInput"
    BankExpenseLinkResult:
      type: object
      required: [bankTransactionId, operatingExpenseId, cashTransactionId, journalEntryId]
      properties:
        bankTransactionId: { type: integer, minimum: 1 }
        operatingExpenseId: { type: integer, minimum: 1 }
        cashTransactionId: { type: integer, minimum: 1 }
        journalEntryId: { type: integer, minimum: 1 }
    ExistingFixedAssetLink:
      type: object
      required: [fixedAssetId]
      properties:
        fixedAssetId: { type: integer, minimum: 1 }
    NewFixedAssetBankPurchaseInput:
      type: object
      required: [name, unitPrice, quantity, date]
      properties:
        name: { type: string, minLength: 1 }
        unitPrice: { type: number, exclusiveMinimum: 0 }
        quantity: { type: integer, minimum: 1 }
        date: { type: string, pattern: '^\d{4}-\d{2}-\d{2}$' }
    BankFixedAssetLinkInput:
      anyOf:
        - $ref: "#/components/schemas/ExistingFixedAssetLink"
        - $ref: "#/components/schemas/NewFixedAssetBankPurchaseInput"
    BankFixedAssetLinkResult:
      type: object
      required: [bankTransactionId, fixedAssetId, cashTransactionId, journalEntryId]
      properties:
        bankTransactionId: { type: integer, minimum: 1 }
        fixedAssetId: { type: integer, minimum: 1 }
        cashTransactionId: { type: integer, minimum: 1 }
        journalEntryId: { type: integer, minimum: 1 }
    BankTransactionJournalPostInput:
      type: object
      required: [accountId]
      properties:
        accountId: { type: integer, minimum: 1 }
    BankTransactionJournalPostResult:
      type: object
      required: [id, journalEntryId]
      properties:
        id: { type: integer }
        journalEntryId: { type: integer }
    BankTransactionImportResult:
      type: object
      required: [imported, skippedDuplicate, skippedZero, totalRead, recognized, unrecognized, transactionIds]
      properties:
        imported: { type: integer, minimum: 0 }
        skippedDuplicate: { type: integer, minimum: 0 }
        skippedZero: { type: integer, minimum: 0 }
        totalRead: { type: integer, minimum: 0 }
        recognized: { type: integer, minimum: 0 }
        unrecognized: { type: integer, minimum: 0 }
        transactionIds:
          type: array
          items: { type: integer, minimum: 1 }
    InventoryPurchaseItem:
      type: object
      required: [id, inventoryItemId, name, category, unit, quantity, unitPrice, totalAmount]
      properties:
        id: { type: integer }
        inventoryItemId: { type: integer }
        name: { type: string }
        category: { type: string }
        unit: { type: string }
        quantity: { type: number }
        unitPrice: { type: number }
        totalAmount: { type: number }
    InventoryPurchase:
      type: object
      required: [id, materialType, accountId, accountCode, accountName, supplierName, hasReceipt, date, totalAmount, paid, createdAt, editable, items]
      properties:
        id: { type: integer }
        materialType:
          type: string
          enum: [food, supply]
        accountId: { type: integer, nullable: true }
        accountCode: { type: string, nullable: true }
        accountName: { type: string, nullable: true }
        supplierName: { type: string }
        hasReceipt: { type: boolean }
        date: { type: string }
        totalAmount: { type: number }
        paid: { type: boolean }
        paymentDate: { type: string, nullable: true }
        paymentAmount: { type: number, nullable: true }
        createdAt: { type: string }
        editable: { type: boolean }
        items:
          type: array
          items:
            $ref: "#/components/schemas/InventoryPurchaseItem"
    InventoryPurchaseItemInput:
      type: object
      required: [name, category, unit, quantity, unitPrice]
      properties:
        inventoryItemId: { type: integer }
        name: { type: string, minLength: 1 }
        category: { type: string, minLength: 1 }
        unit:
          type: string
          enum: [ширхэг, кг, грамм, литр, мл, метр, багц, хайрцаг]
        quantity: { type: number, exclusiveMinimum: 0 }
        unitPrice: { type: number, minimum: 0 }
    InventoryPurchaseInput:
      type: object
      required: [materialType, supplierName, hasReceipt, date, items]
      properties:
        materialType:
          type: string
          enum: [food, supply]
        supplierName: { type: string, minLength: 1 }
        hasReceipt: { type: boolean }
        date:
          type: string
          pattern: '^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$'
        items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/InventoryPurchaseItemInput"
    InventoryPurchasePaymentInput:
      type: object
      required: [date, amount]
      properties:
        date:
          type: string
          pattern: '^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$'
        amount: { type: number, exclusiveMinimum: 0 }
        bankTransactionId: { type: ["integer", "null"] }
    InventoryPurchaseBankSuggestion:
      type: object
      required: [id, transactionAt, amount, description, score]
      properties:
        id: { type: integer }
        transactionAt: { type: string, format: date-time }
        amount: { type: number }
        description: { type: string }
        score: { type: number }
    ReclassifyInventoryPurchaseAsExpenseInput:
      type: object
      required: [accountId]
      properties:
        accountId: { type: integer, minimum: 1 }
    InventorySupplierUpdate:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1 }
    InventorySupplierItem:
      type: object
      required: [name, unit, quantity, totalAmount]
      properties:
        name: { type: string }
        unit: { type: string }
        quantity: { type: number }
        totalAmount: { type: number }
    InventorySupplier:
      type: object
      required: [id, name, purchaseCount, totalAmount, unpaidAmount, items]
      properties:
        id: { type: integer }
        name: { type: string }
        purchaseCount: { type: integer }
        totalAmount: { type: number }
        unpaidAmount: { type: number, description: "totalAmount minus what has actually been paid across this supplier's purchases" }
        items:
          type: array
          items:
            $ref: "#/components/schemas/InventorySupplierItem"
    InventoryItem:
      type: object
      required: [id, materialType, name, category, unit, quantity, totalValue, createdAt]
      properties:
        id: { type: integer }
        materialType:
          type: string
          enum: [food, supply]
        name: { type: string }
        category: { type: string }
        unit: { type: string }
        quantity: { type: number }
        totalValue: { type: number, description: "FIFO value of remaining stock (sum of unconsumed purchase lots at their original unit price)" }
        createdAt: { type: string }
    InventoryItemUpdate:
      type: object
      required: [name, category]
      properties:
        name: { type: string, minLength: 1 }
        category: { type: string, minLength: 1 }
    InventoryIssue:
      type: object
      required: [id, inventoryItemId, itemName, unit, date, quantity, totalCost, purpose, createdAt]
      properties:
        id: { type: integer }
        inventoryItemId: { type: integer }
        itemName: { type: string }
        unit: { type: string }
        date: { type: string }
        quantity: { type: number }
        totalCost: { type: number, description: "FIFO cost of the stock consumed by this issue" }
        purpose: { type: string }
        createdAt: { type: string }
    InventoryIssueInput:
      type: object
      required: [inventoryItemId, date, quantity, purpose]
      properties:
        inventoryItemId: { type: integer }
        date:
          type: string
          pattern: '^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$'
        quantity: { type: number, exclusiveMinimum: 0 }
        purpose: { type: string, minLength: 1 }
    OperatingExpense:
      type: object
      required: [id, description, category, accountId, date, amount, paymentDate, paymentAmount, bankTransactionId, cashTransactionId, createdAt]
      properties:
        id: { type: integer }
        description: { type: string }
        category: { type: string }
        accountId: { type: integer }
        date: { type: string, pattern: '^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$' }
        amount: { type: number, minimum: 0 }
        paymentDate: { type: ["string", "null"] }
        paymentAmount: { type: ["number", "null"] }
        bankTransactionId: { type: ["integer", "null"] }
        cashTransactionId: { type: ["integer", "null"] }
        createdAt: { type: string, format: date-time }
    OperatingExpenseInput:
      type: object
      required: [description, accountId, date, amount]
      properties:
        description: { type: string, minLength: 1 }
        accountId: { type: integer, minimum: 1 }
        date: { type: string, pattern: '^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$' }
        amount: { type: number, exclusiveMinimum: 0 }
    OperatingExpensePaymentInput:
      type: object
      required: [date, amount]
      properties:
        date: { type: string, pattern: '^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$' }
        amount: { type: number, exclusiveMinimum: 0 }
        bankTransactionId: { type: integer, minimum: 1 }
    OperatingExpenseBankSuggestion:
      type: object
      required: [id, transactionAt, amount, description, score]
      properties:
        id: { type: integer }
        transactionAt: { type: string, format: date-time }
        amount: { type: number }
        description: { type: string }
        score: { type: number }
    FixedAsset:
      type: object
      required: [id, name, unitPrice, quantity, totalAmount, date, purchased, bankTransactionId, createdAt]
      properties:
        id: { type: integer }
        name: { type: string }
        unitPrice: { type: number }
        quantity: { type: integer }
        totalAmount: { type: number }
        date: { type: string }
        purchased: { type: boolean }
        bankTransactionId: { type: ["integer", "null"], minimum: 1 }
        createdAt: { type: string }
    FixedAssetInput:
      type: object
      required: [name, unitPrice, quantity, date, purchased]
      properties:
        name: { type: string, minLength: 1 }
        unitPrice: { type: number, exclusiveMinimum: 0 }
        quantity: { type: integer, minimum: 1 }
        date:
          type: string
          pattern: '^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$'
        purchased: { type: boolean }
    DeletionRequest:
      type: object
      required: [id, targetPath, label, requesterRole, status, requestedAt]
      properties:
        id: { type: integer }
        targetPath: { type: string }
        label: { type: string }
        requesterRole: { type: string, enum: [admin, hr, accountant, warehouse] }
        status: { type: string, enum: [pending, executing, completed, failed, cancelled] }
        requestedAt: { type: string }
        approvedAt: { type: ["string", "null"] }
        completedAt: { type: ["string", "null"] }
        error: { type: ["string", "null"] }
    DeletionRequestInput:
      type: object
      required: [targetPath, label]
      properties:
        targetPath: { type: string, minLength: 1 }
        label: { type: string, minLength: 1 }

